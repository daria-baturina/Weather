import {CircularProgress} from "@material-ui/core";

const Spinner = () => {
  return (
    <div>
      <CircularProgress />
      Looking to the window...Wait...
    </div>
  );
};

export default Spinner;